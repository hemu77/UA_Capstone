# Step 3 Method Study Summary

## Verification
- 72 of 72 generated method-study graphs passed the artifact and sanity checks.

## Method effects
- `iterative` produced the highest average density (0.182).
- `global` produced the lowest average density (0.056).
- The most similar method pair was `global vs local` with average edge distance 0.101.
- The most different method pair was `iterative vs local` with average edge distance 0.102.

## Dominant demographics
- `global` most often elevated `age` as the strongest homophily dimension (5 conditions).
- `iterative` most often elevated `political affiliation` as the strongest homophily dimension (9 conditions).
- `local` most often elevated `political affiliation` as the strongest homophily dimension (8 conditions).

## Model consistency within methods
- Across Step 3, the closest model pair was `gpt-4.1 vs gpt-4.1-mini` (0.074).
- The farthest model pair was `gpt-4.1 vs gpt-4.1-nano` (0.119).
